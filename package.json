{
  "name": "debounce-ctx",
  "version": "1.0.3",
  "description": "A simple debounce utility that optionally takes a specific context",
  "main": "index.js",
  "jsdelivr": "./dist/debounce-ctx.min.js",
  "unpkg": "./dist/debounce-ctx.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -fR dist && browserify --standalone debounce index.js -t uglifyify -o dist/debounce-ctx.min.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/minagawah/debounce-ctx.git"
  },
  "keywords": [
    "debounce"
  ],
  "author": "minagawah <minagawa@tokyo800.jp>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/minagawah/debounce-ctx/issues"
  },
  "homepage": "https://github.com/minagawah/debounce-ctx#readme",
  "devDependencies": {
    "browserify": "^16.2.3",
    "uglifyify": "^5.0.1"
  }
}
